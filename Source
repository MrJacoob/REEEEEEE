local list = {
    "I killed a man idiots",

}

for index, value in pairs(list) do
    game:GetService("ReplicatedStorage")["DefaultChatSystemChatEvents"].SayMessageRequest:FireServer(value, "All")
end
